node_modules
.next
out
tsconfig.tsbuildinfo
.env
.env.local
.env*.local
.DS_Store
*.log
.vercel

# Local PDF text extracts used only during build
.pdf-text

# Source brand documents are kept locally only.
# The Information Memorandum contains commercially sensitive financial data.
# Keep them out of the repo by default; share them via secure channels.
source-docs

# Aqualogic photography — commit images no larger than 6MB each. Full-size
# RAW/original files break Vercel deploys, so resize before adding.

# Editor
.vscode
.idea

# Local doc-to-PDF helper (not part of the app)
.md2pdf.py
.collab.html
