node_modules
.next
out
tsconfig.tsbuildinfo
.env
.env.local
.env*.local
.DS_Store
*.log
.vercel

# Local PDF text extracts used only during build
.pdf-text

# Source brand documents are kept locally only.
# The Information Memorandum contains commercially sensitive financial data.
# Keep them out of the repo by default; share them via secure channels.
source-docs

# Aqualogic photography originals are too large for the Vercel deploy
# (they exceeded the size limit). Keep them locally; the folder README is
# tracked so the drop-spot stays documented. Serve photography another way
# (e.g. the photo library) rather than committing originals here.
public/assets/aqualogic/photography/*
!public/assets/aqualogic/photography/README.md

# Editor
.vscode
.idea

# Local doc-to-PDF helper (not part of the app)
.md2pdf.py
.collab.html
